# 了解什么是cookie及cookie的属性
# 能够从客户端的请求中获取cookie 的值 (request.COOKIES.get('key'))
# 能够在回应中设置cookie (response.set_cookie)
# 能够在回应中删除cookie (response.delete_cookie)
# 能够实现一个基本的用户注册/登入/登出功能 (p1)
# 了解什么是session (服务端存储数据的机制)
# 能够启用Django的session功能 (INSTALLED_APPS, MIDDLEWARE)
# 能够读取session中的数据 (request.session.get(key))
# 能够设置session中的数据 (request.session[key] = value)
# 能够删除session中的数据 (del request.session[key])
# 能够手动触发session数据的保存 (request.session.modified = True, p2)
